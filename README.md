# GateKeeper-API-Gateway
asynchronous API gateway built with FastAPI, redis.asyncio, and httpx .

It implements API key authentication, sliding-window rate limiting, request tracing (Correlation IDs) .